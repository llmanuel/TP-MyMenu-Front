import React from 'react';

import styles from './styles.module.scss';

const NavBar = () => (
  <div className={styles.navBar}>
      <h1 className={styles.title}>My Menu App</h1>
  </div>
);

export default NavBar;